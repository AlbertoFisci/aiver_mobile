import 'package:flutter/material.dart';

class AppColors {
  // 🌟 Colore primario AiVer: blu + gradazioni
  static const Color primary = Color(0xFF005BAA); // Blu principale AiVer
  static const Color primaryLight = Color(0xFF4A90E2);
  static const Color primaryDark = Color(0xFF003E77);

  // 🖼️ Sfondo
  static const Color background = Color(0xFFFFFFFF); // Bianco puro
  static const Color scaffoldBackground = Color(0xFFF9F9F9); // Bianco leggermente grigio

  // ✍️ Testi
  static const Color textPrimary = Color(0xFF1C1C1C);
  static const Color textSecondary = Color(0xFF4F4F4F);

  // 🔘 Elementi UI neutri
  static const Color surface = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFDDDDDD);
  static const Color icon = Color(0xFF2E2E2E);

  // ✅ Verde e 🔴 Rosso (usati solo se necessario)
  static const Color success = Color(0xFF3BAF7A);
  static const Color error = Color(0xFFE53935);

  // ⚪️ Neutri
  static const Color grayLight = Color(0xFFF0F0F0);
  static const Color grayMedium = Color(0xFFBDBDBD);
  static const Color grayDark = Color(0xFF757575);
}
